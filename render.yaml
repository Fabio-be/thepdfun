services:
  - type: web
    name: thepdf-fun
    env: python
    plan: free
    branch: main

    buildCommand: |
      apt-get update
      apt-get install -y libreoffice
      pip install .

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11